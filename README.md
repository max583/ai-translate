Automatically translates all files in a given directory. 
Specify your directories in Configuration.py and run:
python translate.py

The text is cut into fragments no longer than the specified size in tokens.
